export type User = {
  dob: string;
  id: number;
  role: string;
  latitude: string;
  longitude: string;
  name: string;
  phoneNumber: string;
  username: string;
};

export interface IServices {
  image: string;
  description: string;
  title: string;
  id: string;
}
